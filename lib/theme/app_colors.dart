import 'package:flutter/material.dart';

class AppColors {
  // ========== CORES DE TEXTO ==========
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textDisabled = Colors.white38;

  // ========== CORES DE FUNDO ==========
  static const Color background = Colors.black;
  static const Color fieldBackground = Colors.black;
  static const Color overlayDark = Color.fromARGB(88, 0, 0, 0);

  // ========== CORES DE BORDA ==========
  static const Color borderDefault = Colors.white;
  static const Color borderSuccess = success;
  static const Color borderError = error;

  // ========== CORES DE TEMA ==========
  static const Color primary = Color.fromARGB(255, 210, 95, 255);
  static const Color accent = Color.fromARGB(255, 183, 0, 255);
  static const Color highlight = Color.fromARGB(255, 181, 0, 253);

  // ========== CORES DE STATUS ==========
  static const Color success = Color.fromARGB(255, 0, 255, 65);
  static const Color error = Color.fromARGB(255, 255, 51, 51);
  static const Color warning = Color.fromARGB(255, 255, 170, 0);
  static const Color info = Color.fromARGB(255, 0, 170, 255);
}